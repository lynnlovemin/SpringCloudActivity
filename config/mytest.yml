spring:
  application:
    name: test
server:
  port: 9999
eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://admin:admin123@localhost:8101/eureka/
