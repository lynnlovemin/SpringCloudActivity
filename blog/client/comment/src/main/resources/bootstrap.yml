spring:
  cloud:
    config:
      name: eurekaclient,comment,datasource,redis,rabbitmq,encrypt
      label: master
      discovery:
        enabled: true
        serviceId: config
      username: admin
      password: admin
eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin123@localhost:8100/eureka/